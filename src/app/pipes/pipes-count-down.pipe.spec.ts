import { PipesCountDownPipe } from './pipes-count-down.pipe';

describe('PipesCountDownPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesCountDownPipe();
    expect(pipe).toBeTruthy();
  });
});
